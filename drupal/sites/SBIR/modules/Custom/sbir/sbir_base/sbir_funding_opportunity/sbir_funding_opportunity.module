<?php

/**
 * Implements hook_ctools_plugin_api().
 */
function sbir_funding_opportunity_ctools_plugin_api($module = NULL, $api = NULL) {
  if ($module == "field_group" && $api == "field_group") {
    return array("version" => "1");
  }
}

/**
 * Implements hook_node_info().
 */
function sbir_funding_opportunity_node_info() {
  $items = array(
    'sbir_funding_opportunity' => array(
      'name' => t('Funding Opportunity'),
      'base' => 'node_content',
      'description' => t('The funding contract being awarded to the grantee'),
      'has_title' => '1',
      'title_label' => t('Title'),
      'help' => '',
    ),
  );
  drupal_alter('node_info', $items);
  return $items;
}
