<?php

/**
 * Implements hook_ctools_plugin_api().
 */
function sbir_event_ctools_plugin_api($module = NULL, $api = NULL) {
  if ($module == "field_group" && $api == "field_group") {
    return array("version" => "1");
  }
}

/**
 * Implements hook_node_info().
 */
function sbir_event_node_info() {
  $items = array(
    'sbir_event' => array(
      'name' => t('Event'),
      'base' => 'node_content',
      'description' => t('Used to store information about various SBIR-related events.'),
      'has_title' => '1',
      'title_label' => t('Title'),
      'help' => '',
    ),
  );
  drupal_alter('node_info', $items);
  return $items;
}
