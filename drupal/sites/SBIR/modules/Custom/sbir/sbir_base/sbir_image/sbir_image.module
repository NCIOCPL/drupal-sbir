<?php

/**
 * Implements hook_ctools_plugin_api().
 */
function sbir_image_ctools_plugin_api($module = NULL, $api = NULL) {
  if ($module == "field_group" && $api == "field_group") {
    return array("version" => "1");
  }
}

/**
 * Implements hook_node_info().
 */
function sbir_image_node_info() {
  $items = array(
    'sbir_image' => array(
      'name' => t('Image'),
      'base' => 'node_content',
      'description' => t('This content type is used to upload images.'),
      'has_title' => '1',
      'title_label' => t('Title'),
      'help' => '',
    ),
  );
  drupal_alter('node_info', $items);
  return $items;
}
